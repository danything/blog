# Secret artalk-secrets は Infisical (il.doany.io) が持ち、External Secrets Operator が
# フォルダ /blog/artalk-secrets の中身をそのままキーにして作る。
# 値の変更は Infisical 側で行い、Git には何も入れない (danything/bootstrap の README 参照)。
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: artalk-secrets
  namespace: blog
spec:
  refreshInterval: 1h
  secretStoreRef:
    kind: ClusterSecretStore
    name: infisical
  target:
    name: artalk-secrets
  dataFrom:
    - find:
        path: /blog/artalk-secrets
        name:
          regexp: .*
